import pytest&#10;from src.models import Question, Quiz&#10;&#10;@pytest.fixture&#10;def sample_question():&#10;    return Question(text="What is 2+2?", options=["3", "4", "5", "6"], correct_index=1)&#10;&#10;def test_question_model(sample_question):&#10;    assert sample_question.text == "What is 2+2?"&#10;    assert len(sample_question.options) == 4&#10;    assert sample_question.correct_index == 1&#10;&#10;def test_quiz_model():&#10;    questions = [Question(text="Q1", options=["A","B"], correct_index=0)]&#10;    quiz = Quiz(topic="test", questions=questions)&#10;    assert quiz.topic == "test"&#10;    assert len(quiz.questions) == 1&#10;&#10;if __name__ == "__main__":&#10;    pytest.main(["-v"])
